## Building

```bash
cargo build            # debug
cargo build --release  # release — binary at target/release/aven
```

## Testing

```bash
cargo test
```

## Filing issues

GitHub Issues on this repo. For aven-guard (Python SDK) issues, file at aven-lang/aven-guard.
